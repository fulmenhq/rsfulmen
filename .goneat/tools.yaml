# goneat Tools Configuration (v1.0.0)
# Generated by: goneat doctor tools init
#
# This file is the SINGLE SOURCE OF TRUTH for tools configuration.
# There are NO hidden defaults or runtime merging.
#
# Edit this file to:
# - Add/remove tools
# - Change tool detection commands
# - Modify installer priorities
# - Define custom scopes
#
# Validate changes with: goneat doctor tools validate
# See schema: schemas/tools/tools.v1.0.0.json
scopes:
  all:
    description: All tools from all scopes
    tools:
      - ripgrep
      - jq
      - yq
      - minisign
      - yamlfmt
      - yamllint
      - prettier
      - gitleaks
      - syft
  foundation:
    description: Core foundation tools required for goneat and basic AI agent operation
    tools:
      - ripgrep
      - jq
      - yq
      - minisign
      - yamlfmt
      - yamllint
      - prettier
  sbom:
    description: Software Bill of Materials (SBOM) generation tools
    tools:
      - syft
  security:
    description: Security scanning tools
    tools:
      - gitleaks
  rust:
    description: Rust development and quality tools
    tools:
      - cargo-tarpaulin
      - cargo-audit
      - cargo-deny
tools:
  gitleaks:
    name: gitleaks
    description: Git secrets scanner
    kind: go
    detect_command: gitleaks version
    install_package: github.com/zricethezav/gitleaks/v8@latest
    version_args:
      - version
    check_args:
      - help
    installer_priority:
      all:
        - go-install
  jq:
    name: jq
    description: JSON processor - used in CI/CD scripts
    kind: system
    detect_command: jq --version
    platforms:
      - darwin
      - linux
      - windows
    installer_priority:
      darwin:
        - brew
      linux:
        - brew
      windows:
        - scoop
        - winget
  minisign:
    name: minisign
    description: Minimal signing utility for release artifacts
    kind: system
    detect_command: minisign -v
    platforms:
      - darwin
      - linux
      - windows
    installer_priority:
      darwin:
        - brew
      linux:
        - brew
      windows:
        - scoop
  prettier:
    name: prettier
    description: Opinionated code formatter
    kind: node
    detect_command: prettier --version
    installer_priority:
      darwin:
        - brew
      linux:
        - brew
      windows:
        - bun
        - npm
  ripgrep:
    name: ripgrep
    description: Fast text search - used by goneat for license auditing
    kind: system
    detect_command: rg --version
    platforms:
      - darwin
      - linux
      - windows
    installer_priority:
      darwin:
        - brew
      linux:
        - brew
      windows:
        - scoop
        - winget
  syft:
    name: syft
    description: SBOM generator for software supply chain security
    kind: system
    detect_command: syft version
    version_args:
      - version
    check_args:
      - --help
    platforms:
      - darwin
      - linux
      - windows
    installer_priority:
      darwin:
        - brew
      linux:
        - brew
      windows:
        - scoop
  yamlfmt:
    name: yamlfmt
    description: YAML formatter and linter
    kind: go
    detect_command: yamlfmt --version
    install_package: github.com/google/yamlfmt/cmd/yamlfmt@latest
    version_args:
      - --version
    check_args:
      - --help
    installer_priority:
      all:
        - go-install
  yamllint:
    name: yamllint
    description: YAML syntax linting (GPL) - install only, never embedded
    kind: system
    detect_command: yamllint --version
    platforms:
      - darwin
      - linux
      - windows
    installer_priority:
      darwin:
        - brew
      linux:
        - brew
      windows:
        - scoop
  yq:
    name: yq
    description: YAML processor
    kind: system
    detect_command: yq --version
    platforms:
      - darwin
      - linux
      - windows
    installer_priority:
      darwin:
        - brew
      linux:
        - brew
      windows:
        - scoop
        - winget
  cargo-tarpaulin:
    name: cargo-tarpaulin
    description: Code coverage reporting for Rust
    kind: cargo
    detect_command: cargo tarpaulin --version
    install_package: cargo-tarpaulin
    version_args:
      - --version
    check_args:
      - --help
    installer_priority:
      all:
        - cargo-install
  cargo-audit:
    name: cargo-audit
    description: Audit Cargo.lock for security vulnerabilities
    kind: cargo
    detect_command: cargo audit --version
    install_package: cargo-audit
    version_args:
      - --version
    check_args:
      - --help
    installer_priority:
      all:
        - cargo-install
  cargo-deny:
    name: cargo-deny
    description: Cargo plugin for linting dependencies (licenses, bans, advisories)
    kind: cargo
    detect_command: cargo deny --version
    install_package: cargo-deny
    version_args:
      - --version
    check_args:
      - --help
    installer_priority:
      all:
        - cargo-install
