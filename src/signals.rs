//! Signals
//!
//! Convenience module that re-exports the signal catalog and helpers from
//! `rsfulmen::foundry::signals`.
//!
//! This exists to align with the platform module registry (`signals` module).

pub use crate::foundry::signals::*;
